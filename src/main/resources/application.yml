
server:
  port: 8080

spring:
  application:
    name: sql-query-migrator
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
      enabled: true

logging:
  level:
    com.querymanager: DEBUG
    root: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

query-migrator:
  temp-directory: ${java.io.tmpdir}/query-migrator

# Zuora Configuration for Playwright Automation
zuora:
  loginUrl: https://one.zuora.com/one-id/login
  username: ${ZUORA_USERNAME:abhijith.rao+sbx@estuate.com}
  password: ${ZUORA_PASSWORD:Estuate@2025}
  tenantId: SBX
  dataQueryUrl: https://apisandbox.zuora.com/platform/apps/data-query/saved-queries
  outputPath: reports/zuora/queries_extract.xlsx
  inputPath: reports/zuora/queries_to_update.xlsx


